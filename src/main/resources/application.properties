spring.application.name=moneytracker

## MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/moneytracker
spring.datasource.username=root
spring.datasource.password=25082002

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true



# Server
server.port=8080

server.servlet.context-path=/api/

#EMAIL CONFIGURATION
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${brevo_login}
spring.mail.password=${smtp_key}
app.mail.from=${T_email}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.protocol=smtp

money.manager.frontend.url=${frontEnd-url}
jwt-secret-key=${secert-key}
backendapp.activation.url=${Money-manger-backendaccountactivationurl}

#spring.profiles.active=prod